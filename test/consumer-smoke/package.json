{
  "name": "consumer-smoke",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "next build"
  },
  "dependencies": {
    "next": "15.5.4",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "devDependencies": {
    "@types/node": "20.x",
    "@types/react": "19.0.0",
    "@types/react-dom": "19.0.0",
    "typescript": "5.6.3"
  },
  "comments": {
    "install": "CI injects the just-built tarball via `npm install ../../FILE.tgz --no-save`. The package ships ESM + preserved JSX — Next requires `transpilePackages` (see next.config.mjs)."
  }
}
