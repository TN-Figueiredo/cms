{
  "name": "consumer-smoke",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "next build"
  },
  "dependencies": {
    "next": "15.5.4",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "comments": {
    "install": "The extracted repo CI runs `npm pack` in the repo root then `npm install ../*.tgz --no-save` in THIS directory to link the just-built tarball (@tn-figueiredo/cms). Do not add the @tn-figueiredo/cms dep here — it is injected by the workflow."
  }
}
